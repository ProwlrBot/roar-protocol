[package]
name = "roar-sdk"
version = "0.3.2"
edition = "2021"
description = "Rust SDK for the ROAR Protocol"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
uuid = { version = "1", features = ["v4"] }
